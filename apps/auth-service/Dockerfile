# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @my-commerce/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t my-commerce-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system my-commerce-auth-service && \
          adduser --system -G my-commerce-auth-service my-commerce-auth-service

COPY apps/auth-service/dist my-commerce-auth-service/
COPY apps/auth-service/package.json my-commerce-auth-service/
RUN chown -R my-commerce-auth-service:my-commerce-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix my-commerce-auth-service --omit=dev -f install

CMD [ "node", "my-commerce-auth-service" ]
